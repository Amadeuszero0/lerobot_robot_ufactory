[build-system]
requires = ["setuptools>=68.0", "wheel>=0.42"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_robot_ufactory_piper_history_ed30ad0"
version = "0.1.0+ed30ad0"
description = "隔离运行的 ed30ad0 历史 Piper joint-stream 实验版本"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
dependencies = [
    "lerobot==0.4.3",
    "numpy>=1.24",
    "pyyaml",
    "piper_sdk>=0.3.0",
    "wego_piper",
]

[project.scripts]
uf-piper-history-ed30ad0-record = "lerobot_robot_ufactory_piper_history_ed30ad0.scripts.record:main"
uf-piper-history-ed30ad0-teleop = "lerobot_robot_ufactory_piper_history_ed30ad0.scripts.teleoperate:main"
uf-piper-history-ed30ad0-check-config = "lerobot_robot_ufactory_piper_history_ed30ad0.scripts.check_config:main"

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.8"]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]

